package com.arjunsports.contentagent.modules.user;

/**
 * Single ADMIN role for phase 1. New roles are added here and picked up
 * automatically by {@code hasRole(...)} checks — no other code changes
 * needed to introduce e.g. EDITOR later.
 */
public enum Role {
    ADMIN
}
