app:
  jwt:
    secret: test-secret-that-is-at-least-32-characters-long-for-hmac-sha
  security:
    # 32 zero bytes, base64-encoded - fine for tests, never used for anything real.
    encryption-key: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=
  cookie:
    secure: false
  cors:
    allowed-origins: http://localhost:3000
  admin-bootstrap:
    email: ""
    password: ""
  storage:
    endpoint: http://localhost:9000
    access-key: test-access-key
    secret-key: test-secret-key
    bucket: test-bucket
